{
  "geode": "5.10.1",
  "gd": {
    "win": "2.2081"
  },
  "id": "thiago7tv.autobot-t7",
  "name": "AutoBot T7",
  "version": "0.1.1",
  "developer": "Thiago7TV",
  "description": "AutoBot T7 V0.1: general realtime solver with runtime CollisionWorld, per-mode physics, MPC replanning and trajectory visualization.",
  "settings": {
    "collision-debug-overlay": {
      "type": "bool",
      "name": "Collision Debug Overlay",
      "description": "Shows Collision World object bounds and query metrics. Exact gameplay hitboxes remain NOT VERIFIED.",
      "default": true
    },
    "collision-trace-debug": {
      "type": "bool",
      "name": "Collision Trace Debug",
      "description": "Expensive diagnostic mode. Traces nearby objects from StaticWorld through primitive creation, SpatialHash, query and renderer, and runs periodic invariants/parse stability checks.",
      "default": false
    },
    "collision-object-labels": {
      "type": "bool",
      "name": "Collision Object Labels",
      "description": "When Collision Trace Debug is enabled, labels nearby objects with source index, object ID, raw GameObjectType and final classification.",
      "default": false
    },
    "collision-debug-full-world": {
      "type": "bool",
      "name": "Collision Debug Draw Mode - FULL WORLD",
      "description": "Diagnostic only. OFF = QUERY ONLY. ON = draw CollisionWorld objects visible by the camera using a brute-force camera-region scan, independent of SpatialHash/queryRegion.",
      "default": false
    },
    "autobot-enabled": {
      "type": "bool",
      "name": "AutoBot Enabled",
      "description": "Enables the realtime General Solver. Input is emitted only after game state, world, mode physics and planner readiness gates are all satisfied.",
      "default": true
    },
    "show-trajectory": {
      "type": "bool",
      "name": "Show Trajectory",
      "description": "Draws candidate trajectories and the selected path directly from the same TrajectorySimulator results consumed by the planner.",
      "default": true
    },
    "solver-debug": {
      "type": "bool",
      "name": "Solver Debug",
      "description": "Shows detailed solver scores, predictions, model error and planner timing without changing solver decisions.",
      "default": false
    }
  }
}
