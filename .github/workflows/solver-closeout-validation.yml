name: Solver Closeout Validation

on:
  push:
    branches:
      - solver-closeout

permissions:
  contents: read

concurrency:
  group: solver-closeout-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Diff check
        shell: bash
        run: |
          set -euo pipefail
          git diff --check origin/main...HEAD
          echo "GIT_DIFF_CHECK=PASS"

      - name: Install Ninja
        shell: bash
        run: |
          set -euo pipefail
          curl -L https://github.com/ninja-build/ninja/releases/latest/download/ninja-win.zip -o ninja.zip
          7z x ninja.zip -o"$GITHUB_WORKSPACE/ninja"
          echo "$GITHUB_WORKSPACE/ninja" >> "$GITHUB_PATH"

      - name: Setup Geode CLI
        uses: geode-sdk/cli/.github/actions/setup@main
        with:
          version: v3.7.4

      - name: Setup Geode SDK
        shell: bash
        run: |
          set -euo pipefail
          export CLI_PROFILE="${{ github.workspace }}/cli-profile"
          mkdir -p "$CLI_PROFILE/geode/mods" "$CLI_PROFILE/Contents/geode/mods"
          geode profile add --name GithubActions "$CLI_PROFILE/GeometryDash.exe" win
          geode sdk install "${{ github.workspace }}/geode-sdk-clone"
          export GEODE_SDK="${{ github.workspace }}/geode-sdk-clone"
          echo "GEODE_SDK=$GEODE_SDK" >> "$GITHUB_ENV"
          geode sdk update "5.10.1"
          geode sdk install-binaries --platform win --version "5.10.1"

      - name: Search and runtime regression tests
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ci-tests
          CXX=(clang++ -std=c++23 -Wall -Wextra -Werror -Iinclude)

          "${CXX[@]}" tests/UniversalSearchCoreTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/universal-search.exe
          ./ci-tests/universal-search.exe

          "${CXX[@]}" tests/UniversalMechanicsSuiteTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/universal-suite.exe
          ./ci-tests/universal-suite.exe

          "${CXX[@]}" tests/SearchStateAliasTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/search-alias.exe
          ./ci-tests/search-alias.exe

          "${CXX[@]}" tests/InfiniteWaitDominanceTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/infinite-wait.exe
          ./ci-tests/infinite-wait.exe

          "${CXX[@]}" tests/UniversalDesyncRecoveryTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/desync-recovery.exe
          ./ci-tests/desync-recovery.exe

          "${CXX[@]}" tests/UniversalSearchRuntimeLivenessTest.cpp src/presolve/UniversalSearchRuntimeLiveness.cpp -o ci-tests/liveness.exe
          ./ci-tests/liveness.exe

          "${CXX[@]}" tests/UniversalMacroSearchPerformanceTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/macro-perf.exe
          ./ci-tests/macro-perf.exe

          "${CXX[@]}" tests/IncrementalExpansionBudgetTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/incremental-budget.exe
          ./ci-tests/incremental-budget.exe

          "${CXX[@]}" tests/RuntimeSearchBootstrapTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/runtime-bootstrap.exe
          ./ci-tests/runtime-bootstrap.exe

          "${CXX[@]}" tests/LiveSearchRoundtripTest.cpp src/presolve/UniversalSearchCore.cpp -o ci-tests/live-roundtrip.exe
          ./ci-tests/live-roundtrip.exe

          SHADOW_COMMON=(
            src/presolve/ShadowUniversalOracle.cpp
            src/presolve/UniversalSearchCore.cpp
            src/solver/PhysicsModels.cpp
            src/solver/PhysicsValidationHarness.cpp
            src/world/CollisionWorld.cpp
            src/world/SpatialHash.cpp
          )

          "${CXX[@]}" tests/NoInternalGDUpdateDuringSearchTest.cpp "${SHADOW_COMMON[@]}" -o ci-tests/no-internal-gd-update.exe
          ./ci-tests/no-internal-gd-update.exe

          "${CXX[@]}" tests/ShadowSimDeterminismTest.cpp "${SHADOW_COMMON[@]}" -o ci-tests/shadow-determinism.exe
          ./ci-tests/shadow-determinism.exe

      - name: Search/live-engine boundary audit
        shell: python
        run: |
          from pathlib import Path

          search_paths = [
              "src/presolve/UniversalSearchCore.cpp",
              "src/presolve/UniversalRuntimeSession.cpp",
              "src/presolve/ShadowUniversalOracle.cpp",
              "src/solver/RealtimePlanner.cpp",
              "src/solver/TrajectorySimulator.cpp",
              "src/solver/GlobalPlanner.cpp",
          ]

          forbidden = [
              "GJBaseGameLayer::update",
              "PlayLayer::update",
              "createCheckpoint",
              "loadFromCheckpoint",
              "GeometryDashRuntimeOracle",
          ]

          for path in search_paths:
              text = Path(path).read_text(encoding="utf-8")
              for token in forbidden:
                  if token in text:
                      raise SystemExit(f"FORBIDDEN SEARCH ENGINE PATH: {path}: {token}")

          hook = Path("src/hooks/GameLayerHook.cpp").read_text(encoding="utf-8")
          if "setStepCallback" in hook or "g_universalInternalStep" in hook or "innerDt" in hook:
              raise SystemExit("LIVE HOOK STILL CONTAINS INTERNAL GD SIMULATION PATH")

          start = hook.index("class $modify(AutoBotT7BaseGameLayerHook, GJBaseGameLayer)")
          end = hook.index("class $modify(AutoBotT7GameLayerHook, PlayLayer)", start)
          base_hook = hook[start:end]

          calls = base_hook.count("GJBaseGameLayer::update(dt);")
          if calls != 2:
              raise SystemExit(f"EXPECTED TWO SOURCE CALLS (owned path + fallback), FOUND {calls}")

          owned = base_hook.split("if (universalOwns)", 1)[1].split("return;", 1)[0]
          if owned.count("GJBaseGameLayer::update(dt);") != 1:
              raise SystemExit("OWNED LIVE FRAME DOES NOT CONTAIN EXACTLY ONE REAL UPDATE")

          if "searchSlice" in base_hook or "ShadowUniversalOracle" in base_hook:
              raise SystemExit("SEARCH WORK MUST NOT RUN INSIDE GJBaseGameLayer::update")

          print("NO_INTERNAL_GD_UPDATE_DURING_SEARCH_SOURCE_AUDIT=PASS")
          print("ONE_REAL_UPDATE_PER_FRAME_TEST=PASS")

      - name: Existing solver gates
        shell: bash
        run: |
          set -euo pipefail
          CXX=(clang++ -std=c++23 -Wall -Wextra -Werror -Iinclude)
          COMMON=(
            src/presolve/PreRunSolver.cpp
            src/world/SpatialHash.cpp src/world/CollisionWorld.cpp src/world/DynamicWorldModel.cpp src/world/TriggerWorldModel.cpp
            src/solver/LocalWorldView.cpp src/solver/GeneralWorldModel.cpp src/solver/GlobalPlanner.cpp src/solver/AdaptiveSearchBudget.cpp
            src/solver/PhysicsValidationHarness.cpp src/solver/PhysicsModels.cpp src/solver/TrajectorySimulator.cpp src/solver/TrajectoryScorer.cpp
            src/solver/ActionGenerator.cpp src/solver/DualJointPlanner.cpp src/solver/RealtimePlanner.cpp src/control/AutonomousTestDriver.cpp
          )
          "${CXX[@]}" tests/ModelTruthTest.cpp "${COMMON[@]}" -o ci-tests/model-truth.exe
          ./ci-tests/model-truth.exe
          "${CXX[@]}" tests/ActionCountdownTest.cpp "${COMMON[@]}" -o ci-tests/action-countdown.exe
          ./ci-tests/action-countdown.exe

      - name: Configure real Geode mod
        shell: bash
        run: |
          set -euo pipefail
          cmake -B build             -DCMAKE_BUILD_TYPE=Release             -DCMAKE_C_COMPILER=clang             -DCMAKE_CXX_COMPILER=clang++             '-DGEODE_CODEGEN_CMAKE_ARGS=-DCMAKE_C_COMPILER=clang;-DCMAKE_CXX_COMPILER=clang++;-G Ninja'             -G Ninja             -DGEODE_DONT_INSTALL_MODS=ON             -DGEODE_TARGET_PLATFORM=Win64             '-DCMAKE_CXX_FLAGS=-Wall -Wextra'

      - name: Compile and package internally
        shell: bash
        run: |
          set -euo pipefail
          cmake --build build --config Release --verbose --parallel 2
          test -n "$(find build -type f -name 'thiago7tv.autobot-t7.dll' -print -quit)"
          test -n "$(find build -type f -name '*.geode' -print -quit)"
          echo "SOLVER_CLOSEOUT_INTERNAL_BUILD=PASS"

      - name: Prepare runtime candidate
        shell: bash
        run: |
          set -euo pipefail
          GEODE_SOURCE="$(find build -type f -name '*.geode' -print -quit)"
          test -n "$GEODE_SOURCE"

          mkdir -p runtime-candidate
          cp "$GEODE_SOURCE" runtime-candidate/AutoBot-T7-runtime-candidate.geode
          SHA256="$(sha256sum runtime-candidate/AutoBot-T7-runtime-candidate.geode | awk '{print $1}')"

          cat > runtime-candidate/RUNTIME-CANDIDATE-REPORT.txt <<EOF
          AUTOBOT T7 — RUNTIME CANDIDATE
          FINAL_SOLVER_CLOSEOUT_COMMIT=${GITHUB_SHA}
          RUN_ID=${GITHUB_RUN_ID}
          SOURCE_BRANCH=${GITHUB_REF_NAME}
          SHA256=${SHA256}

          BUILD SYSTEM = PASS
          SOLVER-CLOSEOUT CI = PASS
          LIVE SOLVER = COMPILED

          GENERAL SOLVER GAMEPLAY = PENDING USER RUNTIME
          STEREO MADNESS 100% = PENDING USER RUNTIME

          UNIVERSAL GD SOLVER = NOT VERIFIED
          VERSION ACCEPTANCE = OPEN
          FAILED RUNTIME BUILDS = 0 / 5

          PUBLIC RELEASE = NO
          GEODE INDEX = UNTOUCHED
          EOF

          printf '%s\n' "$GITHUB_SHA" > runtime-candidate/SOURCE-COMMIT.txt
          sha256sum runtime-candidate/AutoBot-T7-runtime-candidate.geode > runtime-candidate/SHA256SUMS.txt

          echo "RUNTIME_CANDIDATE_SHA256=$SHA256"
          echo "RUNTIME_CANDIDATE_PREPARED=PASS"

      - name: Upload runtime candidate
        uses: actions/upload-artifact@v4
        with:
          name: AutoBot-T7-runtime-candidate
          path: runtime-candidate/
          if-no-files-found: error
          retention-days: 14
